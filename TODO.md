# TODO for Vercel Deployment Setup

- [ ] Edit app.py to remove the `if __name__ == '__main__': app.run(debug=True)` block for serverless compatibility.
- [ ] Create vercel.json with builds and routes configuration for Vercel deployment.
